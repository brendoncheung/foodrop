import 'package:flutter/material.dart';

class PromotionExpired extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: Container(child: Text("Expired")));
  }
}
