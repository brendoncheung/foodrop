import 'package:flutter/material.dart';

class PromotionCurrent extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: Container(child: Text("Current")));
  }
}
